{
  "name": "velo-compute",
  "version": "0.1.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build:wasm": "cd rust && wasm-pack build --target web --out-dir ../js/wasm",
    "build:js": "tsc",
    "build": "npm run build:wasm && npm run build:js",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "bench": "tsx benchmarks/sort.bench.ts"
  },
  "devDependencies": {
    "@types/benchmark": "^2.1.5",
    "@types/jest": "^29.5.0",
    "@types/node": "^20.0.0",
    "benchmark": "^2.1.4",
    "jest": "^29.0.0",
    "ts-jest": "^29.1.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
